## What and why

<!-- One or two sentences: what changed, why it was needed. -->

## Checklist

- [ ] Tests added or updated for the behavior change
- [ ] `PYTHONPATH=src python3 -m pytest tests/ -q` passes locally
- [ ] `PYTHONPATH=src python3 -m pyflakes src/ tests/ examples/` is clean
- [ ] Docs (`README.md`, docstrings) updated if public behavior changed
